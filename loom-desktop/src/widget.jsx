import React from "react";
import ReactDOM from "react-dom/client";
import QuickCaptureWidget from "./QuickCaptureWidget.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <QuickCaptureWidget />
  </React.StrictMode>
);
